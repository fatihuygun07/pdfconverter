<Page x:Class="PdfConverter.DropZonePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#ECF0F1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20,20,20,10">
            <Button Content="← Geri" Click="BackButton_Click" Width="80" Height="35" Background="#95A5A6" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            <TextBlock x:Name="TitleText" Text="İşlem" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" Margin="20,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="50" BorderBrush="#3498DB" BorderThickness="3" CornerRadius="10" 
                Background="White" AllowDrop="True" Drop="DropZone_Drop" DragEnter="DropZone_DragEnter" DragLeave="DropZone_DragLeave"
                MouseLeftButtonDown="DropZone_Click" Cursor="Hand">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="📁" FontSize="80" HorizontalAlignment="Center" Foreground="#3498DB"/>
                <TextBlock Text="Dosyaları Buraya Sürükleyin" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#2C3E50" Margin="0,20,0,5"/>
                <TextBlock Text="veya Seçmek İçin Tıklayın" FontSize="16" HorizontalAlignment="Center" Foreground="#7F8C8D"/>
                <TextBlock x:Name="FileCountText" Text="" FontSize="14" HorizontalAlignment="Center" Foreground="#27AE60" Margin="0,20,0,0" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Margin="20,10,20,30">
            <ProgressBar x:Name="ProgressBar" Height="25" Visibility="Collapsed" Foreground="#3498DB"/>
            <TextBlock x:Name="ProgressText" Text="" FontSize="14" HorizontalAlignment="Center" Margin="0,10,0,0" Foreground="#2C3E50"/>
            <Button x:Name="ProcessButton" Content="İşlemi Başlat" Click="ProcessButton_Click" Width="200" Height="45" 
                    Background="#3498DB" Foreground="White" FontSize="16" BorderThickness="0" Cursor="Hand" Margin="0,10,0,0" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</Page>
